# Render Web Service – يُستخدم عند ربط المستودع أو إعداد الخدمة
services:
  - type: web
    name: roadmapapp
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server.js
