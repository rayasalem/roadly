# انسخ هذا الملف إلى .env واملأ القيم. لا ترفع .env إلى Git.
# Copy this file to .env and fill in values. Do not commit .env to Git.

# عنوان API السيرفر (مثال للإنتاج)
# API base URL (e.g. for production)
EXPO_PUBLIC_API_URL=https://api.your-domain.com/api

# بيئة التشغيل (اختياري): development | preview | production
# EXPO_PUBLIC_ENV=production
