/**
 * Design system entry: layout tokens, breakpoints, nav tab config.
 */
export { BREAKPOINT_DESKTOP, CONTENT_MAX_WIDTH, SIDE_NAV_WIDTH, isDesktopWeb } from './layout';
export { NAV_TABS, type NavTabId, type NavTabConfig } from '../navigation/navTabs';
export {
  AmbientSurface,
  DashboardShell,
  DashboardSectionHeader,
  DashboardEmptyState,
  standardSheetBackground,
  standardSheetContentPadding,
} from '../components/dashboard';
